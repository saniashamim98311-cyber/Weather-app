<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather App</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #74ebd5, #ACB6E5);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    h1 {
      color: #fff;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.3);
    }

    .weather-container {
      background: white;
      border-radius: 15px;
      padding: 20px;
      width: 300px;
      text-align: center;
      box-shadow: 0px 5px 20px rgba(0, 0, 0, 0.2);
    }

    input {
      width: 80%;
      padding: 10px;
      border: 1px solid #ddd;
      border-radius: 10px;
      margin-bottom: 10px;
    }

    button {
      background: #4CAF50;
      color: white;
      padding: 10px 15px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-weight: bold;
    }

    button:hover {
      background: #45a049;
    }

    .weather-result {
      margin-top: 15px;
      display: none;
    }

    .temp {
      font-size: 2rem;
      font-weight: bold;
    }

    .condition {
      font-size: 1.2rem;
      margin: 5px 0;
    }

    img {
      width: 60px;
      height: 60px;
    }

    .error {
      color: red;
      font-weight: bold;
      margin-top: 10px;
    }
  </style>
</head>
<body>

  <h1>🌤️ Weather App</h1>
  <div class="weather-container">
    <input type="text" id="locationInput" placeholder="Enter location...">
    <button onclick="getWeather()">Get Weather</button>
    <div class="error" id="errorMsg"></div>
    <div class="weather-result" id="weatherResult">
      <div class="temp" id="temperature"></div>
      <div class="condition" id="condition"></div>
      <img id="icon" alt="Weather Icon">
    </div>
  </div>

  <script>
    async function getWeather() {
      const location = document.getElementById("locationInput").value;
      const errorMsg = document.getElementById("errorMsg");
      const weatherResult = document.getElementById("weatherResult");

      if (!location) {
        errorMsg.textContent = "Please enter a location!";
        weatherResult.style.display = "none";
        return;
      }

      errorMsg.textContent = "";
      try {
        const response = await fetch(`http://api.weatherapi.com/v1/current.json?key=7cdf92da00a54766acb90210252909&q=${location}&aqi=yes`);
        if (!response.ok) {
          throw new Error("Location not found");
        }
        const data = await response.json();

        document.getElementById("temperature").textContent = `${data.current.temp_c}°C`;
        document.getElementById("condition").textContent = data.current.condition.text;
        document.getElementById("icon").src = data.current.condition.icon;

        weatherResult.style.display = "block";
      } catch (error) {
        errorMsg.textContent = "❌ Could not fetch weather data. Check location.";
        weatherResult.style.display = "none";
      }
    }
  </script>

</body>
</html>
